# One workspace for the SDK and AgentInc app

Import the existing native Mac app into this repository, beside the SDK, as ADR 0001 intended. Preserve its assets, history, data migration path and native acceptance checks; do not copy the SDK into a second repository. The exact crate names are pending the separate crate-naming job.
